package com.alrdream.domain.workspace.domain;

/**
 * [04_milestone.md] Phase 04 — 소프트 삭제(deleted_at)와 별개인 워크스페이스 자체의 상태.
 * 현재는 값이 하나뿐이라 실질적인 분기는 없지만, 향후 상태가 늘어날 것을 대비해 String이 아닌 enum으로 둔다.
 */
public enum WorkspaceStatus {
	ACTIVE
}
